package user

import (
	"context"

	"github.com/santowijaya28/cleanarch-sample/internal/src/model"
)

// Register will fo registration process for a user
func (u *Usecase) Register(ctx context.Context, userData model.UserData) (err error) {
	// TODO: complete logic
	return
}
